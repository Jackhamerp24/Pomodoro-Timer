
// For now this is empty, but it's good practice to have it.
// You can use the contextBridge to securely expose Node.js APIs
// to your React application if you need them.
// Example:
//
// const { contextBridge, ipcRenderer } = require('electron')
// contextBridge.exposeInMainWorld('myAPI', {
//   doAThing: () => ipcRenderer.send('do-a-thing')
// })
